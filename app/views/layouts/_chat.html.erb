<div class="container text-center">
  <div class="row">
    <div class="chat-button">
      <button id="addClass"><span class="glyphicon glyphicon-comment"></span><%= t "open_chat" %></button>
    </div>
    <hr>
  </div>
</div>

<div class="popup-box chat-popup" id="qnimate">
  <div class="popup-head">
    <div class="popup-head-left pull-left">
      <%= image_tag image_url("http://bootsnipp.com/img/avatars/bcf1c0d13e5500875fdd5a7e8ad9752ee16e7462.jpg"), alt: "iamgurdeeposahan" %>
      Group 3 <!-- Group chưa tạo. Nên lấy tạm 1 group id để tét chát. -->
    </div>
    <div class="popup-head-right pull-right">
      <div class="btn-group">
      </div>
      <button data-widget="remove" id="removeClass" class="chat-header-button pull-right" type="button"><i class="glyphicon glyphicon-off"></i></button>
    </div>
  </div>
  <% @chatroom = Chatroom.find_by id: 3 %><!-- Group chưa tạo. Nên lấy tạm 1 group id để tét chát. -->
  <div class="popup-messages" id="scroll">
    <div class="direct-chat-messages">
      <% messages = @chatroom.messages.last(5) %>
      <% if messages.any? %>
        <div class="card-body" id="messages">
          <%= render partial: "messages/message", collection: messages%>
        </div>
      <%else%>
        <div class="card-body hidden" id="messages"></div>
      <%end%>
    </div>
  </div>
  <div class="popup-messages-footer">
    <%= render partial: "messages/message_form", locals: {message: @message, chatroom: @chatroom}%>
  </div>
</div>
